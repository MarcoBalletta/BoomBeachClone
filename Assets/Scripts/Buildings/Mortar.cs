using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mortar : Defense
{
    //public override Enemy GetTarget()
    //{
    //    if (targets.Count == 0) return null;
    //    //check if target[0] is too close to mortar, checks the next one in case, or return null
    //}
}
